pydantic>=2.0
PyYAML>=6.0
rich>=13.0
httpx>=0.27
websockets>=12.0
numpy>=1.26
pytest>=8.0
# Optional: convenience wrapper for the StatsAPI. This build calls the REST
# feed directly via httpx, so it is not required.
# MLB-StatsAPI>=1.7
